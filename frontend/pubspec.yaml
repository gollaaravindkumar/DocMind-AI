name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.5.0+5

environment:
  sdk: ^3.9.0

dependencies:
  audioplayers: ^6.6.0
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
